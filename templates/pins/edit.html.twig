{% extends 'base.html.twig' %}

{% block title %}
    PANTEREST - Edit pin #{{ pin.id }}
{% endblock %}

{% block body %}
    <article>
        <h1>Edit pin #{{ pin.id }}</h1>

        <a href="{{ path('app_home') }}">
            Go back
        </a>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Edit a pin" formnovalidate/>
        {{ form_end(form) }}
    </article>
{% endblock %}
