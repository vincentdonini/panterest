{% extends 'layouts/base.html.twig' %}

{% block title %}
    PANTEREST - Edit pin #{{ pin.id }}
{% endblock %}

{% block body %}
    <article>
        <h1>Edit pin #{{ pin.id }}</h1>

        <a href="{{ path('app_home') }}">
            Go back
        </a>

        {{ include('pins/partials/_form.html.twig', {'sumitButtontext': 'Update pin #' ~ pin.id}) }}
    </article>
{% endblock %}
