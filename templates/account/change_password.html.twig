{% extends 'layouts/base.html.twig' %}

{% block title %}
    PANTEREST - Change password profile
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            <article>

                <div class="text-center mb-5">
                    <h1>
                        Update profile
                    </h1>
                </div>

                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <div class="d-grid">
                        <input class="btn btn-primary" type="submit" value="{{ sumitButtontext | default('Apply changes') }}" formnovalidate/>
                    </div>
                {{ form_end(form) }}

            </article>
        </div>
    </div>
{% endblock %}

